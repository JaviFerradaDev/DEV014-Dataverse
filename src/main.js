import { renderItems } from './view.js';
import data from './data/dataset.js';

renderItems(data);

//--import { example } from './dataFunctions.js'; 

//Todo tu código que tenga que ver con mostrar los datos en la pantalla. 
//Interacción con el DOM (creación de nodos, registro de manejadores de eventos (event listeners o event handlers).
//En este archivo encontrarás una serie de imports listos para cargar las diferentes fuentes de datos.
//Por ejemplo, los datos con los que vas a trabajar, los encontrarás en la siguiente línea: import data from './data/dataset.js';

//import { renderItems } from './view.js';

//import data from './data/dataset.js';

//console.log(example, renderItems(data), data);


